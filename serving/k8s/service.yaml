apiVersion: v1
kind: Service
metadata:
  name: fraud-detector-service
  namespace: fraud-detection
spec:
  type: NodePort
  selector:
    app: fraud-detector
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
